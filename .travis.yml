language: python
python:
  # We don't actually use the Travis Python, but this keeps it organized.
  - "3.5"
  - "3.6"
install:
  - bash -x libs/travis-conda-scripts/scripts/install.sh
script:
  - echo "No tests to run..."
after_success:
  - bash -x libs/travis-conda-scripts/scripts/install.sh
  - bash -x libs/travis-conda-scripts/scripts/upload.sh
deploy:
  skip_cleanup: true
